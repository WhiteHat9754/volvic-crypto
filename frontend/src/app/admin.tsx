import axios from 'axios';

function Admin() {
  const handleAdjustProfit = () => {
    // Admin-only action
  };

  return (
    <div>
      <h2 className="text-xl font-semibold mb-4">Admin Panel</h2>
      {/* User management, profit adjustment UI */}
    </div>
  );
}

export default Admin;
